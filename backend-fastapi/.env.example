# Sol & Stone — AI services (spec §15)

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Federal Reserve (FRED) key for the live market ticker.
# Unset in the demo: /ai/ticker reads ticker/values.json instead (spec §4.2).
# Register for a free key at https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=
